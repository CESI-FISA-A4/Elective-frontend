import axios from "axios";

export const submitContact = async (firstname, lastname, email, message, role, ) => {
    try {
        return true; 
    } catch (error) {
        console.error(error);
    }
}